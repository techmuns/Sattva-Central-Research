name: NSE and BSE bulk/block refresh

on:
  schedule:
    # Half-hourly 09:35–22:05 IST, including post-close publication and corrections.
    - cron: '5,35 4-16 * * 1-5'
    # Weekend catch-up for late publications.
    - cron: '35 13 * * 0,6'
  workflow_dispatch:
    inputs:
      source:
        description: 'Refresh requester'
        required: false
        default: 'manual'

concurrency:
  group: exchange-deals-capture
  cancel-in-progress: false

permissions:
  contents: read
  actions: read

jobs:
  capture:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: '22'
      - name: Capture complete NSE exports and BSE historical reports
        id: capture
        env:
          FAMILY_HOLDINGS_LIVE: 'true'
          GH_TOKEN: ${{ github.token }}
        run: node scripts/capture-exchange-deals.mjs
      - name: Retain merged history and source status
        if: always() && hashFiles('tmp/exchange-capture/exchange-deals.json.gz') != ''
        uses: actions/upload-artifact@v5
        with:
          name: exchange-deals
          path: tmp/exchange-capture/exchange-deals.json.gz
          compression-level: 0
          retention-days: 90
          if-no-files-found: error
